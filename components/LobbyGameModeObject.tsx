import { View, Text } from 'react-native'
import React from 'react'

// This is the component that will show in the list of games
// Displays the index? gamemode and an x to delete it off the list

const LobbyGameModeObject = () => {
  return (
    <View >

    </View>
  )
}

export default LobbyGameModeObject